#include <iostream>

using namespace std;

int main()
{
    int age = 19;
    int money = 1000;
    int x = 41;
    if(age > 21 || money > 500 && x==40){
        cout << "You are allowed in.";
    }
    return(0);
}
