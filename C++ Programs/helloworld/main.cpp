#include <iostream>

using namespace std;
//These two lines carry a similar file to <stdio.h> in c

int main()
{
    cout << "Hello world! " << endl; //<< endl (end line) is like \n in c
    //stream that prints characters to the screen (object?)

    return(0);
}
